# Secondary judge — the second member of the two-model judge panel. A different
# family from the primary judge so the two don't share blind spots. Native
# OpenAI provider (reads OPENAI_API_KEY, no gateway).
id: openai:chat:gpt-5.5
